{
  "name": "hussayn-signal-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute hussayn-signal-db --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute hussayn-signal-db --file=./schema.sql --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.65.0"
  }
}